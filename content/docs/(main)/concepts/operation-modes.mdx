---
title: Operation Modes
description: Understand how local integrated servers and remote dedicated servers fit into the SoulFire model.
icon: Waypoints
---

import { Mermaid } from '../../../../src/components/mdx/mermaid';

## One product, two roles

SoulFire has a client and a server, like Minecraft.
You use the client to control bots.
The server runs the bot connections.

## Integrated server (local)

The GUI and CLI include an integrated SoulFire server.
It runs bots on your computer without a separate server setup.
Only the client that started the integrated server can use it.
For remote access, use a dedicated server.

<Mermaid chart="
graph LR
    subgraph Y[Your machine]
        C[SoulFire client]
        I[SoulFire integrated server]
    end
    subgraph M[Minecraft Server]
    end
    C --> I
    I --> M
" />

This is the recommended mode for most first-time users.

## Dedicated server (remote)

A dedicated server runs SoulFire separately from the GUI or CLI.
Use it to run bots on one machine and control them from another.
Several users and clients can connect to the same server.
You can also run several dedicated servers on one or more machines.

<Mermaid chart="
graph LR
    subgraph Y[Your machine]
        YourClient[SoulFire client]
    end
    subgraph O[Other user's machine]
        OtherClient[SoulFire client]
    end
    subgraph R[Remote machine]
        S[SoulFire dedicated server]
    end
    subgraph M[Minecraft Server]
    end
    YourClient --> S
    OtherClient --> S
    S --> M
" />

## How GUI and CLI fit in

- The **GUI** is best when you want visual control, configuration screens, plugins, and scripting tools.
- The **CLI** is best when you want terminal automation, headless runs, or SSH workflows.
- Both can run locally with an integrated server or connect to a remote dedicated server.

## Choose a mode

- Choose the integrated server when you want the simplest local setup.
- Choose a dedicated server when you want a long-running backend, remote access, or multiple clients on the same backend.
- Choose the CLI when you want automation or a keyboard-only workflow.

## Related pages

- [Choose Your Setup](/docs/start-here/choose-your-setup) for the practical decision guide
- [Run Your First Bots](/docs/start-here/gui-mode) for the beginner path
- [Deploy a Dedicated Server](/docs/start-here/dedicated-server) for the automated remote setup
