---
title: Choose an automation layer
description: Choose how to automate SoulFire bots with commands, visual scripts, SDKs, durable tasks, or the beat-game runner.
icon: Bot
---

import { Bot, Blocks, Code2, Terminal, Workflow } from 'lucide-react';
import { Cards, Card } from 'fumadocs-ui/components/card';
import { Callout } from 'fumadocs-ui/components/callout';

SoulFire provides several automation options.
Choose the simplest option that can do your task.

| Goal | Use |
| --- | --- |
| Run one operator action | [Commands](/docs/reference/commands) |
| React to chat, timers, or events in the GUI | [Visual scripting](/docs/scripting) |
| Run a long task that continues after your program disconnects | [Durable tasks](/docs/sdk/tasks) |
| Build a TypeScript or Python program | [Official SDK](/docs/sdk) |
| Automate Minecraft game progress | [Beat-game runner](/docs/sdk/automation) |
| Add new server features | [Plugins](/docs/sdk/plugins) |

## What SoulFire manages

SoulFire manages Minecraft connections, pathfinding, bot input, and reusable tasks.
Your application chooses the goals and combines these features.

The beat-game planner runs in `@soulfiremc/beat-game`.
It does not run inside the SoulFire server or SoulFireClient.
The TypeScript worker can resume saved progress, coordinate teams, and call plugin APIs.
You can change its plan without changing SoulFire.

<Callout type="info">
  SoulFireClient does not contain a native beat-game dashboard. Run and
  monitor the application from your TypeScript worker, logs, or your own UI.
</Callout>

## Tasks and decisions

A collect-blocks task only collects blocks.
The worker decides when to run it and how many blocks the team needs.

The same rule applies to plugins.
A plugin can add a typed server feature.
The application decides when to use that feature.

## Related pages

<Cards>
  <Card icon={<Workflow />} title="Beat-game runner" href="/docs/sdk/automation">
    Save and resume game progress for one bot or a team.
  </Card>
  <Card icon={<Code2 />} title="Official SDK" href="/docs/sdk">
    Build Effect, Promise, async Python, or synchronous Python applications.
  </Card>
  <Card icon={<Blocks />} title="Durable tasks" href="/docs/sdk/tasks">
    Use general-purpose work that runs beside the Minecraft game loop.
  </Card>
  <Card icon={<Terminal />} title="Commands" href="/docs/reference/commands">
    Run one operator action.
  </Card>
  <Card icon={<Bot />} title="Visual scripting" href="/docs/scripting">
    Connect triggers and actions in the GUI.
  </Card>
</Cards>
